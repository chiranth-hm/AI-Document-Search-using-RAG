# Required: free token from https://huggingface.co/settings/tokens
HUGGINGFACEHUB_API_TOKEN=your_token_here

# Optional: if chat fails with "model not supported", try another (see https://huggingface.co/inference/models)
# CHAT_MODEL=Qwen/Qwen2.5-7B-Instruct
# CHAT_MODEL=meta-llama/Llama-3.2-3B-Instruct
